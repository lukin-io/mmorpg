<% if record.errors.any? %>
  <div class="nl-manage-errors" role="alert">
    <strong><%= pluralize(record.errors.count, "error") %> prevented this change:</strong>
    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
