<h2>Captured modes</h2>
<div class="nl-shop-tabs">
  <% building["modes"].each do |mode| %>
    <span class="nl-arena-tab"><%= mode %></span>
  <% end %>
</div>
<p class="nl-profile-empty">Stock was not captured and is intentionally not copied from the General Shop.</p>
