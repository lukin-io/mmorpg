<h2>Captured modes</h2>
<div class="nl-city-building-tabs">
  <% building["modes"].each do |mode| %>
    <span class="nl-city-building-tab"><%= mode %></span>
  <% end %>
</div>
<p class="nl-empty-note">Stock was not captured and is intentionally not copied from the General Shop.</p>
