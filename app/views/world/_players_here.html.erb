<div class="players-panel">
  <h3>Players Here</h3>
  <% if players.any? %>
    <ul class="players-list">
      <% players.each do |pos| %>
        <li class="player-item">
          <span class="player-avatar">👤</span>
          <div class="player-info">
            <span class="player-name"><%= pos.character.name %></span>
            <span class="player-level">Lv.<%= pos.character.level %></span>
          </div>
          <div class="player-actions">
            <%= link_to "👁️", profile_path(pos.character.user.profile_name), title: "View Profile" %>
          </div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-muted text-sm">No other players nearby.</p>
  <% end %>
</div>

